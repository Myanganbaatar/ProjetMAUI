<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BarsboldApp.Views.AjoutPage"
             Title="Ajout"
             BackgroundColor="White">
    
    <ScrollView>
        <VerticalStackLayout
            Padding="24,32,24,32"
            Spacing="20">

            <!-- Titre de la page -->
            <Label
                Text="Ajouter un élément"
                FontSize="26"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="#1A1A1A"
                Margin="0,0,0,32" />

            <!-- Champ Titre -->
            <VerticalStackLayout Spacing="10">
                <Label
                    Text="Titre"
                    FontSize="13"
                    FontAttributes="Bold"
                    TextColor="#3A3A3C" />

                <Frame
                    Padding="16,12"
                    HasShadow="False"
                    BorderColor="Transparent"
                    BackgroundColor="#F0F0F5"
                    CornerRadius="15">
                    <Entry
                        x:Name="TitreEntry"
                        Text="{Binding Titre}"
                        Placeholder="Entrez le titre"
                        PlaceholderColor="#B0B0B5"
                        FontSize="16"
                        TextColor="#1A1A1A"
                        BackgroundColor="Transparent" />
                </Frame>
            </VerticalStackLayout>

            <!-- Champ Description -->
            <VerticalStackLayout Spacing="10">
                <Label
                    Text="Description"
                    FontSize="13"
                    FontAttributes="Bold"
                    TextColor="#3A3A3C" />

                <Frame
                    Padding="16,12"
                    HasShadow="False"
                    BorderColor="Transparent"
                    BackgroundColor="#F0F0F5"
                    CornerRadius="15">
                    <Editor
                        x:Name="DescriptionEditor"
                        Text="{Binding Description}"
                        Placeholder="Entrez la description"
                        PlaceholderColor="#B0B0B5"
                        FontSize="16"
                        TextColor="#1A1A1A"
                        BackgroundColor="Transparent"
                        HeightRequest="100"
                        AutoSize="TextChanges" />
                </Frame>
            </VerticalStackLayout>

            <!-- Bouton Photo -->
            <Button
                Text="📷  Ajouter une photo"
                Command="{Binding ChoisirImageCommand}"
                BackgroundColor="#586B95"
                TextColor="White"
                CornerRadius="25"
                HeightRequest="50"
                FontSize="15"
                FontAttributes="Bold"
                Margin="0,8,0,0" />

            <!-- Zone d'aperçu Image -->
            <VerticalStackLayout Spacing="8">
                <!-- Message si aucune image -->
                <Label
                    Text="Aucune image sélectionnée"
                    FontSize="12"
                    TextColor="#B0B0B5"
                    HorizontalOptions="Center"
                    Margin="0,8,0,0"
                    IsVisible="{Binding ImagePath, Converter={StaticResource InverseStringToBoolConverter}}" />

                <!-- Aperçu de l'image -->
                <Frame
                    Padding="0"
                    HasShadow="False"
                    BorderColor="Transparent"
                    BackgroundColor="Transparent"
                    CornerRadius="15"
                    HeightRequest="200"
                    IsVisible="{Binding ImagePath, Converter={StaticResource StringToBoolConverter}}">
                    <Image
                        x:Name="PreviewImage"
                        Source="{Binding ImagePath}"
                        Aspect="AspectFill" />
                </Frame>
            </VerticalStackLayout>

            <!-- Bouton Valider -->
            <Button
                x:Name="BtnAjouter"
                Text="Ajouter l'élément"
                Command="{Binding AjouterCommand}"
                BackgroundColor="#586B95"
                TextColor="White"
                CornerRadius="25"
                HeightRequest="50"
                FontSize="16"
                FontAttributes="Bold"
                Margin="0,24,0,0"
                Pressed="OnAjouterPressed"
                Released="OnAjouterReleased" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
